import React from 'react';

const Table = ({ tournament }) => {
    return (
        <div>
            <h2>Tables for {tournament.name}</h2>
            {/* Add table-related content */}
        </div>
    );
};

export default Table;